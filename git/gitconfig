[user]
    email = jzempel@gmail.com
    name = Jonathan Zempel
[core]
    editor = mvim -f
    excludesfile = ~/.gitignore
[credential]
    helper = osxkeychain
[alias]
    branches = !legit branches
    graft = !legit graft \"$@\"
    harvest = !legit harvest \"$@\"
    pkgtag = !$HOME/Dropbox/Library/pkgtag
    publish = !legit publish \"$@\"
    sprout = !legit sprout \"$@\"
    switch = !legit switch \"$@\"
    sync = !legit sync \"$@\"
    unpublish = !legit unpublish \"$@\"
[color]
    ui = auto
[format]
    pretty = oneline
[github]
    user = jzempel
[github "template"]
	branch-prefix = {{ github.user }}/
[hub]
    protocol = https
[pull]
	rebase = preserve
[push]
    default = current
