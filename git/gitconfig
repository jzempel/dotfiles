[user]
    email = jzempel@gmail.com
    name = Jonathan Zempel
[core]
    editor = code --wait
    excludesfile = ~/.gitignore
[credential]
    helper = osxkeychain
[color]
    ui = auto
[format]
    pretty = oneline
[github]
    user = jzempel
[github "template"]
    branch-prefix = {{ github.user }}/
[hub]
    protocol = https
[include]
    path = /Users/jzempel/.gitconfig.private
[init]
    templatedir = ~/.git
[merge]
    ff = false
[pull]
    rebase = merges
[push]
    default = current
    followtags = true
[remote "origin"]
    prune = true
    prunetags = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
